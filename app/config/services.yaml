# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#  parameter_name: value

services:
    _defaults:
        autowire: false
        autoconfigure: false
        public: true

    logger:
        alias: monolog.logger
        public: true

when@test:
    imports:
        - { resource: "../../tests/Resources/config/services.yaml" }
