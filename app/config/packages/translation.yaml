framework:
    default_locale: "%locale%"
    translator:
        default_path: '%kernel.project_dir%/translations'
        providers:
        fallbacks: [ "default" ]
        paths: '%modules_translation_paths%'
